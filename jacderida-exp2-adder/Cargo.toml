[package]
name = "jacderida-exp2-adder"
version = "0.1.3"
edition = "2018"
description = "Little binary for experimenting with automated release workflows."
license = "MIT OR Apache-2.0"

[dependencies]
jacderida-exp2-add-lib = { version = "^0.1.4", path = "../jacderida-exp2-add-lib" }
