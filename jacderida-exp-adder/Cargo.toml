[package]
name = "jacderida-exp-adder"
version = "0.1.0"
edition = "2018"
description = "Little binary for experimenting with automated release workflows."
license = "MIT OR Apache-2.0"

[dependencies]
jacderida-exp-add-lib = { version = "0.1.0", path = "../jacderida-exp-add-lib" }
